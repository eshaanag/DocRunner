# Acme Client

## Installation

```bash
printf "installed\n"
```

## Quick Start

```python
values = [1, 2, 3]
assert sum(values) == 6
```

### Browser Check

```js
const payload = { ok: true };
if (!payload.ok) throw new Error("not ok");
```
